  #
  system(OverloadedEquals1, javaFacts("ProxyJavaFacts","ua.kiev.gradsoft.JavaChecker.ProxyJavaFacts"),
    ruleset(

      import(general,logical_and),

      FOUND_EQUALS($classname) -> true //  violationDiscovered("OverloadedEquals","overrided equals, but not hashcode",$classname),

      FOUND_HASHCODE($classname) -> true //  violationDiscovered("OverloadedEquals","overrided hashcode, but not equals",$classname)

    ),
    FirstTop
);

